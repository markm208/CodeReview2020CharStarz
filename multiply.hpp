#pragma once
#include "multiply.cpp"

bool multiply(int c1, int n1, int d1, int c2, int n2, int d2, char result[], int len);

int multiplyTwoNums(int num1, int num2);
int genChar(int char1, int char2);
int genNumerator(int num1, int num2);
int genDenominator(int den1, int den2);
char genWholeNum(int char1, int num1);